import React from 'react';
import './Loading.css'

const Loading = () => {
    return (
        <div className="spin">
            <span className="spin-loader"></span>
        </div>
    );
};

export default Loading;